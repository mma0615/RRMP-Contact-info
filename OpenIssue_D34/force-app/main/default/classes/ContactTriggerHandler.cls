public with sharing class ContactTriggerHandler extends TriggerHandler {
//  public override void afterUpdate() { - no longer nesc. Citizenship pushed from app to Contact.
//    ContactService.setCurrentApplicationLawfulPresence(Trigger.new, (Map<Id, Contact>)Trigger.oldMap);
//  }
}